import { TenancyAddConnector } from './tenancy-add-connector';

describe('TenancyAddConnector', () => {
  it('should create an instance', () => {
    expect(new TenancyAddConnector()).toBeTruthy();
  });
});
